import 'package:flutter/material.dart';

void main() {
  runApp(
    new Container(
      color: Colors.red,
      child: new Center(
      child: new Text("Hello World new", textDirection: TextDirection.ltr,)
      )
  )
  );
}